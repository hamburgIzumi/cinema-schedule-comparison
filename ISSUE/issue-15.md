# [Issue #15] ゼブラ縞表示および列境界線の視覚的強化

- **ステータス**: 完了 (Completed)
- **担当**: 役割B (フロントエンド開発)
- **ブランチ**: `feat/issue-15-zebra-column-styling` -> `work`

## 1. 目的
比較マトリクス表において、行のゼブラストライプ（縞模様）表示、縦列の区切り線、タイトル列の固定境目、ホバー時のハイライト表現を強化し、一目で映画館ごとのスケジュールが識別できるように視覚的視認性を向上させる。

## 2. 実装内容
- `css/main.css` & `css/components.css`:
  - `tr:nth-child(even)` に適度なコントラスト背景色（ゼブラ表示）を追加。
  - `td`, `th` の間に縦方向の可視ボーダー（`border-right`）を追加。
  - 作品タイトル列（`col-movie-title`）の境界にアクアブルーの垂直セパレーターとシャドウを追加。
  - `tr:hover` 時の背景ハイライトおよび視認性の向上。
- 実ブラウザ（`browser_subagent`）による描画検証とスクショ確認。

## 3. 完了条件
- [x] ゼブラストライプ（縞表示）が行ごとに交互に適用されていること
- [x] 映画館列の間に明確な縦方向の区切り線が表示されること
- [x] 実ブラウザでのレンダリング確認・テストがエラーなしで完了すること
