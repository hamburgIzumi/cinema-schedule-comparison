# [Issue #8] 各映画館の直接API/データフェッチ方式への切り替えと切り戻し用安全スイッチの実装

- **ステータス**: 切り戻し完了 (Reverted to Original Mode)
- **担当**: 役割B (フロントエンド開発)
- **ブランチ**: `work`

## 1. 目的
公式サイトとの最新データ一致率を高めるためAPI直接取得を試行したのち、検証結果に基づき設定フラグ（`useDirectApi: false`）によって従来の安全な取得動作に切り戻す。

## 2. 実装・切り戻し内容
- `config/cinemas.json`: `useDirectApi` フラグを `false` に変更して従来のフェッチ処理に即座復元
- トグルスイッチ構造による安全な元状態への切り戻し完了

## 3. 完了条件
- [x] `useDirectApi: false` により従来の取得動作に正常復元されていること
- [x] エラー停止等が発生せず動作が維持されること
